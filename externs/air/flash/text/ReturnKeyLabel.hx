package flash.text;

@:native("flash.text.ReturnKeyLabel")
extern enum abstract ReturnKeyLabel(String)
{
	var DEFAULT = "default";
	var DONE = "done";
	var GO = "go";
	var NEXT = "next";
	var SEARCH = "search";
}
